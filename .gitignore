node_modules
dist
dist-ssr
*.local

# Environment (never commit real keys)
.env
.env.*
!.env.example

# Editor / OS
.DS_Store
Thumbs.db
.vscode/*
!.vscode/extensions.json

# Local session state and internal activity log (not for the repo)
.remember/
docs/activity-log.md
scratchpad/
